#!/bin/sh
# Reject tooling metadata in commit messages.
if grep -qiE 'co-authored-by:.*(claude|copilot|bot@)|generated with|🤖' "$1"; then
  echo "commit message contains tooling metadata" >&2
  exit 1
fi
